[package]
name = "immich-launcher"
version = "0.1.0"
edition = "2021"
description = "Immich をローカル専用で安全に起動する Windows 向け GUI ランチャー"

[dependencies]
eframe = "0.29"
rand = "0.8"

[profile.release]
opt-level = 2
lto = true
strip = true
